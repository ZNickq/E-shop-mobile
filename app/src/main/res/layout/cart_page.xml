<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">
    
    <TextView
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:textSize="25dp"
        android:text="Carucior de Cumparaturi"
        android:paddingTop="10dp"
        android:paddingBottom="10dp"
        android:paddingLeft="5dp"
        android:paddingRight="5dp"
        android:id="@+id/titleCartPage"/>


    <ListView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/listViewCartPage"
            android:layout_below="@+id/titleCartPage"
            android:layout_above="@+id/buttonCartPageCheckout"
            android:isScrollContainer="true"
            >
            </ListView>
        <Button
            android:layout_width="80dp"
            android:layout_height="30dp"
            android:id="@+id/buttonCartPageCheckout"
            android:layout_alignParentBottom="true"
            android:layout_alignParentRight="true"
            android:layout_margin="15dp"
            android:background="@color/bluecolor"
            android:textColor="@color/white"
            android:text="Trimite"
            />
</RelativeLayout>